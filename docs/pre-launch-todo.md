# 宣发前 TODO

## 必要

- [ ] **数据来源扩展 / 说明**：当前仅支持 Codex（`~/.codex`），但应用定位是多 Agent。宣发前需二选一：
  - 支持更多 Agent 数据来源（如 Claude Code 等）
  - 或在 README、应用界面内明确标注"当前仅支持 Codex"，避免其他 Agent 用户安装后发现无数据

- [ ] **空数据状态**：用户不存在 `~/.codex` 目录时，界面是否有友好的引导提示？这是新用户的第一印象

## 强烈建议

- [ ] **应用图标**：目前使用默认 Tauri 图标，换成有辨识度的自定义图标，专业感差距显著

- [ ] **README 补充截图**：一张界面截图是吸引用户安装的最重要元素

## 可选

- [ ] **反馈渠道**：在应用内或 README 中提供 GitHub Issues 链接，方便用户在 alpha 阶段提交问题

- [ ] **版本号去掉 alpha 后缀**：`alpha` 标签对普通用户有一定劝退效果，视发布预期决定是否去除
